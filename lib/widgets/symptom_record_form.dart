import 'package:flutter/material.dart';

class SymptomRecordForm extends StatelessWidget {
  const SymptomRecordForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Symptom Record Form'),
    );
  }
}
